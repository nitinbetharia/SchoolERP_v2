<% if (typeof flash !== 'undefined' && flash && flash.message) { %>
  <div class="mb-4 rounded-lg p-4 <%= flash.type === 'error' ? 'bg-red-100 text-red-700' : 'bg-green-100 text-green-700' %>">
    <%= flash.message %>
  </div>
<% } %>
